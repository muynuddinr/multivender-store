import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['developer.apple.com', 'images.unsplash.com', 'images.pexels.com', 'play.google.com'],
  },
};

export default nextConfig;
